name: Rust CI

on:
  pull_request:
    paths:
      - 'editeur_rust/rust-core/**'
  push:
    branches: [main]
    paths:
      - 'editeur_rust/rust-core/**'

jobs:
  test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: editeur_rust/rust-core
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy
      - name: Run tests
        run: cargo test
      - name: Run clippy
        run: cargo clippy -- -D warnings